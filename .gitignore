
# The unrevised owner-supplied architecture source is a build input, not a published asset.
# Only the revised, watermarked diagram is published (DSI-WEBSITE-1B §13).
assets/diagrams/Pluraxis Architecture.PNG
